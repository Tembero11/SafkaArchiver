import * as hash from "object-hash";
import { DbDay } from "./dbTypes";

export function hashMenus(day: DbDay) {
    const hash = "";
    for (let i = 0; i < day.menu.length; i++) {
        return hash
    }
}